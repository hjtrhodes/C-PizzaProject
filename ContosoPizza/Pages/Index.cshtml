@page
@model IndexModel
@{
    ViewData["Title"] = "Oxfordshire's Perfect Pizza!";
    TimeSpan timeInBusiness = DateTime.Now - new DateTime(2024, 4, 1);
}

<div class="text-center">
    <h1 class="display-4">Welcome to SoCo Pizza</h1>
    <p class="lead">The best pizza in town for @Convert.ToInt32(timeInBusiness.TotalDays) days!</p>
</div>
